{
  "name": "semantic-route-assistant-core",
  "version": "0.1.0",
  "description": "Semantic index engine for Semantic Route Assistant.",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "wasm"
  ],
  "scripts": {
    "build": "tsc",
    "postbuild": "mkdir -p dist/wasm && cp wasm/*.wasm dist/wasm/ && cp node_modules/web-tree-sitter/web-tree-sitter.wasm dist/web-tree-sitter.wasm",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "ignore": "^5.3.1",
    "tree-sitter": "^0.22.4",
    "tree-sitter-cpp": "^0.23.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "web-tree-sitter": "^0.26.3"
  }
}
